# AWS Multi-Region Resilience Workshop -- pinned for reproducible builds.
# Python 3.9+ / Amazon Linux 2023 compatible.

Flask==3.0.3
gunicorn==22.0.0
requests==2.32.3

# Database drivers. Both are installed so DB_ENGINE can be flipped between
# mysql and postgresql without rebuilding the instance.
mysql-connector-python==8.4.0
psycopg2-binary==2.9.9

# Transitive dependencies of Flask, pinned explicitly.
Werkzeug==3.0.3
Jinja2==3.1.4
MarkupSafe==2.1.5
itsdangerous==2.2.0
click==8.1.7
blinker==1.8.2
certifi==2024.7.4
charset-normalizer==3.3.2
idna==3.7
urllib3==2.2.2
packaging==24.1
